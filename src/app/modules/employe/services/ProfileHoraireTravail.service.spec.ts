/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ProfileHoraireTravailService } from './ProfileHoraireTravail.service';

describe('Service: ProfileHoraireTravail', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ProfileHoraireTravailService]
    });
  });

  it('should ...', inject([ProfileHoraireTravailService], (service: ProfileHoraireTravailService) => {
    expect(service).toBeTruthy();
  }));
});
